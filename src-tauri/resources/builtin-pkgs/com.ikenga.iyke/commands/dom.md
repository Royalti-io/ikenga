---
description: Print the accessibility tree of the focused pane in the PA desktop app.
---

Run `iyke dom` and surface the result. Refs (`e1`, `e2`, …) are stable until the next snapshot or navigation — use them with `iyke click --ref <ref>`. If the user wants to interact with a specific element, identify the ref from this output.
