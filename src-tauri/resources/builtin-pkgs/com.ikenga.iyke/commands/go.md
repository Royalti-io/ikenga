---
description: Navigate the focused pane of the PA desktop app to a route path.
argument-hint: <route-path>
---

Run `iyke go $ARGUMENTS` to navigate the focused pane. Use this for routes inside the app — e.g. `/finance`, `/sessions`, `/install`. After navigating, run `iyke dom` if the user wants to interact with the new view.
