---
description: Print recent webview console logs from the PA desktop app.
---

Run `iyke logs` and surface the result. Use this to read smoke-test output, debug FE errors, or verify async operations completed. Note: `iyke logs` does NOT support `--since` (broken serde) — always read all + grep client-side.
