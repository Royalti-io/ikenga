---
description: Print the running PA desktop app's current shell state (mode, focused route, panes).
---

Run `iyke state` and report what's currently visible in the desktop app — sidebar mode, focused route, open panes. Use this as the first call when the user asks anything about "what's in the app right now" or before any other iyke command, so you know which pane is focused.
